Crie um modulo python escrito em CPP. Util para melhorar a performance de funções com alto custo computacional

o codigo examplo em C++ fica em /src

para compilar o codigo em C, é necessario um compilador instalado, ex: gcc (linux, osx)

para compilar e instalar:
pip3 install .

execute o teste:
python3 test.py
